// Constants (URLs, Endpoints) Settings

export const CONFIG = {
    USE_MOCK: true, // Set to false when your backend is ready
    BACKEND_URL: "http://localhost:5000",
    ENDPOINTS: {
        LOGIN: "/login",
        SUBSCRIPTIONS: "/api/subscriptions"
    }
};